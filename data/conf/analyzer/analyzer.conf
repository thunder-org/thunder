home-path = "g:/workspace/thunder"
conf-path = ${home-path}"/data/conf/analyzer"

analyzer {
    #conf-file-path = ${conf-path}"/komoran.conf"
    conf-file-path = ${conf-path}"/klay.conf"

    refine {
        remove-url = true
        remove-email = true
    }

    analysis {
        types = ["noun", "number", "english"]
    }
}
